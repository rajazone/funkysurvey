<% @survey.surveyowner=session[:username] %>
<%= form_for(@survey) do |f| %>

  <% if @survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
      <% @survey.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :surveyname %><br />
    <%= f.text_field :surveyname %>
  </div>
   <div class="field">
    <%= f.hidden_field :surveyowner %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
